# __PLUGIN_NAME__

What this plugin is for, in one paragraph a person can read.

## Install

It is already in `plugins/`. Turn it on:

```
os plugin enable __PLUGIN_NAME__
os plugin verify __PLUGIN_NAME__
os help
```

## What it adds

List the commands, the tools, and the files it creates. Be exact. Someone
deciding whether to trust this folder is reading this section.

## Capabilities it asks for

`commands`. Add a line here for every capability in `plugin.json` saying why it
needs it. A capability with no reason written down is a capability to remove.

## Refusals

What it will not do.
