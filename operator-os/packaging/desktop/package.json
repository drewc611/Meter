{
  "name": "operator-os-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Desktop cockpit for an existing Operator OS business folder -- runs the same ./os commands, no terminal required.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
