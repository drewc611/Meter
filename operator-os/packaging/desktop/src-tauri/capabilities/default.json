{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Operator OS cockpit window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
