"""operator-os: `pip install`-able scaffolder for Operator OS.

Not the product itself -- see this package's own README.md. The real tool
lives in the bundle this package carries and is unpacked by `cli.new()`.
"""

__version__ = "1.0.0"
